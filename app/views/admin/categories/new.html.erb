<h1>Add A Category</h1>

<%= form_for(@category, url: admin_categories_path) do |f| %>
  <%= f.label :subject, "Category" %>
  <%= f.text_field :subject %><br />
  <%= f.submit "Make New Category"%>
<% end %>
