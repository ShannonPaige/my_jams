<h1>All Categories</h1>

<% @categories.each do |category| %>
  <%= link_to "#{category.subject} Songs", admin_category_path(category) %>
  <%#= link_to '[edit category]', edit_admin_category_path(category) %>
  <%#= link_to '[delete category]', admin_category_path(category), method: :delete %><br />
<% end %>
<br />
<%= link_to 'Add New Category', new_admin_category_path %>
